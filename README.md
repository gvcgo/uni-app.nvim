# uni-app.nvim
Auto-configures Volar, snippets, and commands for uni-app development.
Requires Neovim 0.11 or newer.

TypeScript projects must provide a valid `typescript` package, for example:

```sh
npm install -D typescript
```
